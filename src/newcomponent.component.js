'use strict'

angular.
  module('newComponent').
  component('newComponent', {
    templateUrl: 'newcomponent.template.html',
    bindings: {
    }
    controller: function () {
      var self = this;
    }
  });
